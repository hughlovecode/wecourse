.course-all
  height:100%;
  width:100%;
  .course-center
    height 100%
    display flex
    align-items center
    justify-content center
    .CourseInfo
      height: calc(100% - 20px)
      padding-top:20px
      width: 100%
      overflow-x  hidden
.course
  display flex
  justify-content space-between
